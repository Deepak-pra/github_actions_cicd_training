name: demo

on:
    push:
        branches:
            - dev

jobs:
    demo:
      name: Demo Job
      runs-on: ubuntu-latest

      steps:
        - name: Code checkout
          uses: actions/checkout@v4

