name: Demo8 - Node Validation and Publish to Docker Hub

on:
  push:
    branches:
      - main
    paths:
      - "app/node/**"
      - ".github/workflows/dockerhub-demo.yml"

permissions:
  contents: read

jobs:
  quality-gates:
    name: Code Quality Gates
    runs-on: ubuntu-latest
    timeout-minutes: 10

    defaults:
      run:
        working-directory: app/node

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: app/node/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Run unit tests
        run: npm run test:unit

      - name: Run integration tests
        run: npm run test:integration

      - name: Generate test coverage
        run: npm run test:coverage

      - name: Run SonarQube Cloud scan
        uses: SonarSource/sonarqube-scan-action@v8
        with:
          projectBaseDir: app/node
          args: >
            -Dsonar.qualitygate.wait=true
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        continue-on-error: true  


  build-scan-publish:
    name: Build, Scan and Publish OCI Image to Docker Hub
    needs: quality-gates
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set image version and name
        shell: bash
        run: |
          echo "IMAGE_VERSION=1.0.${GITHUB_RUN_NUMBER}" >> "${GITHUB_ENV}"
          echo "LOCAL_IMAGE=node-oci-demo:${GITHUB_SHA}" >> "${GITHUB_ENV}"
          echo "DOCKERHUB_IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/node-oci-demo" >> "${GITHUB_ENV}"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build OCI image locally
        uses: docker/build-push-action@v6
        with:
          context: app/node
          file: app/node/Dockerfile
          load: true
          push: false
          tags: node-oci-demo:${{ github.sha }}
          labels: |
            org.opencontainers.image.title=node-oci-demo
            org.opencontainers.image.description=Node.js OCI image demo
            org.opencontainers.image.version=1.0.${{ github.run_number }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
          build-args: |
            APP_VERSION=1.0.${{ github.run_number }}

      - name: Scan OCI image with Trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: node-oci-demo:${{ github.sha }}
          format: table
          exit-code: "1"
          ignore-unfixed: true
          vuln-type: "os,library"
          severity: "CRITICAL"

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Tag and publish image to Docker Hub
        shell: bash
        run: |
          docker tag \
            "${LOCAL_IMAGE}" \
            "${DOCKERHUB_IMAGE}:${IMAGE_VERSION}"

          docker tag \
            "${LOCAL_IMAGE}" \
            "${DOCKERHUB_IMAGE}:latest"

          docker push "${DOCKERHUB_IMAGE}:${IMAGE_VERSION}"
          docker push "${DOCKERHUB_IMAGE}:latest"