name: Demo

on:
  push:
    branches:
      - dev

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Code checkout
        uses: actions/checkout@v4  
      
      
      






          